{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "npm",
      "script": "build",
      "group": "build",
      "problemMatcher": [],
      "label": "npm: build",
      "detail": "rm -rf dist && esbuild `find src -name '*.ts'` --sourcemap --platform=node --target=node18 --format=esm --outdir=dist"
    }
  ]
}
